{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Products</h1>
    <ul>
        {% for product in products %}
            <p> {{ product }}</p>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
    <p> {{ dump(products) }}
{% endblock %}
